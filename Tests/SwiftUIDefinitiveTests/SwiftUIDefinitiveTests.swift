import XCTest
@testable import SwiftUIDefinitive

final class SwiftUIDefinitiveTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIDefinitive().text, "Hello, World!")
    }
}
